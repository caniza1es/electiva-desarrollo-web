<%- include('../partials/header', { pageTitle: 'Jergar.io - Verificación de Email' }) %>

<main>
    <h2>Verificación de Email</h2>

    <% if (messages.success) { %>
        <p><strong><%= messages.success %></strong></p>
        <p>Tu cuenta ha sido verificada exitosamente.</p>
        <p>
            Puedes ahora acceder a tu 
            <% if (user) { %>
                <a href="/users/profile">perfil</a>.
            <% } else { %>
                <a href="/users/login">cuenta</a>.
            <% } %>
        </p>
    <% } else if (messages.error) { %>
        <p><strong><%= messages.error %></strong></p>
        <p>El enlace de verificación es inválido o ha expirado.</p>
        <% if (user) { %> 
            <p>
                Por favor, solicita un nuevo enlace de verificación desde tu 
                <a href="/users/profile">perfil</a>.
            </p>
        <% } else { %>
            <p>
                Intenta registrarte nuevamente o verifica tu correo electrónico para un nuevo enlace.
            </p>
            <p>
                ¿No tienes una cuenta? <a href="/users/register">Regístrate aquí</a>.
            </p>
        <% } %>
    <% } else { %>
        <p>Procesando verificación...</p>
    <% } %>
</main>

<%- include('../partials/footer') %>
